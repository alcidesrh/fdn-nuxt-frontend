'use strict';

var index = {
  gap: '0.25rem',
  icon: {
    size: '1rem',
    color: '{text.muted.color}',
    hoverColor: '{primary.color}',
    activeColor: '{primary.color}'
  }
};

module.exports = index;
