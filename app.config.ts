// @unocss-include
export default defineAppConfig({
    icon: {
        size: '24px', // default <Icon> size applied
        class: 'fdn-icon', // default <Icon> class applied
        mode: 'css', // default <Icon> mode applied
        aliases: {
            nuxt: 'logos:nuxt-icon'
        }
    }
});
